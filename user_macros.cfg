# [gcode_macro PRINT_START]
# #   Use PRINT_START for the slicer starting script - please customize for your slicer of choice
# gcode:
#     G28                            ; home all axes
#     G1 Z20 F3000                   ; move nozzle away from bed
[gcode_macro PRINT_START]
gcode:
    # Parameters
    {% set bedtemp = params.BED|int %}
    {% set hotendtemp = params.HOTEND|int %}

    SAVE_GCODE_STATE NAME=pre_PRINT
    # <insert routines>
    M190 S{bedtemp}                                                              ; wait for bed temp
    # <insert routines>
    M109 S{hotendtemp}                                                           ; wait for hotend temp
    # <insert routines / nozzle clean>

    G28 PROBE_LOCK
    BED_MESH_CALIBRATE
    Dock_Probe_Unlock

    G90                                  ; set position to absolute
    G1 X60 Y0 Z20 F3000                  ; move to front and bed down

    SET_PIN PIN=Nevermore VALUE=1

    # # <insert routines>
    # M190 S{bedtemp}                                                              ; wait for bed temp
    # # <insert routines>
    # M109 S{hotendtemp}                                                           ; wait for hotend temp
    # # <insert routines / nozzle clean>

[gcode_macro PRINT_END]
#   Use PRINT_END for the slicer ending script - please customize for your slicer of choice
gcode:
    M400                           ; wait for buffer to clear
    G92 E0                         ; zero the extruder
    G1 E-4.0 F3600                 ; retract filament
    G91                            ; relative positioning

    #   Get Boundaries
    {% set max_x = printer.configfile.config["stepper_x"]["position_max"]|float %}
    {% set max_y = printer.configfile.config["stepper_y"]["position_max"]|float %}
    {% set max_z = printer.configfile.config["stepper_z"]["position_max"]|float %}

    #   Check end position to determine safe direction to move
    {% if printer.toolhead.position.x < (max_x - 20) %}
        {% set x_safe = 20.0 %}
    {% else %}
        {% set x_safe = -20.0 %}
    {% endif %}

    {% if printer.toolhead.position.y < (max_y - 20) %}
        {% set y_safe = 20.0 %}
    {% else %}
        {% set y_safe = -20.0 %}
    {% endif %}

    {% if printer.toolhead.position.z < (max_z - 2) %}
        {% set z_safe = 2.0 %}
    {% else %}
        {% set z_safe = max_z - printer.toolhead.position.z %}
    {% endif %}

    G0 Z{z_safe} F3600             ; move nozzle up
    G0 X{x_safe} Y{y_safe} F20000  ; move nozzle to remove stringing
    TURN_OFF_HEATERS
    M107                           ; turn off fan
    G90                            ; absolute positioning
    G0 X60 Y{max_y} F3600          ; park nozzle at rear

    UPDATE_DELAYED_GCODE ID=nevermore_shutdown DURATION=300
    RESTORE_GCODE_STATE NAME=pre_PRINT

[delayed_gcode nevermore_shutdown]
gcode:
    SET_PIN PIN=Nevermore VALUE=0

[gcode_macro LOAD_FILAMENT]
gcode:
    SAVE_GCODE_STATE NAME=pre_LOAD_FILLAMENT
    M83                            ; set extruder to relative
    G1 E30 F300                    ; load
    G1 E15 F150                    ; prime nozzle with filament
    M82                            ; set extruder to absolute
    RESTORE_GCODE_STATE NAME=pre_LOAD_FILLAMENT
    
[gcode_macro UNLOAD_FILAMENT]
gcode:
    SAVE_GCODE_STATE NAME=pre_UNLOAD_FILLAMENT
    M83                            ; set extruder to relative
    G1 E10 F300                    ; extrude a little to soften tip
    G1 E-40 F1800                  ; retract some, but not too much or it will jam
    M82                            ; set extruder to absolute
    RESTORE_GCODE_STATE NAME=pre_UNLOAD_FILLAMENT

[gcode_macro PURGE_FILAMENT]
gcode:
    SAVE_GCODE_STATE NAME=pre_PURGE_FILAMENT
    G28
    G1 X60 Y60 Z50 F3000  
    M83                            ; set extruder to relative
    G1 E100 F300                    ; extrude a little to soften tip
    G1 E-2 F1800                  ; retract some, but not too much or it will jam
    M82                            ; set extruder to absolute
    RESTORE_GCODE_STATE NAME=pre_PURGE_FILAMENT

[gcode_macro UNSAFE_PURGE_FILAMENT]
gcode:
    SAVE_GCODE_STATE NAME=pre_UNSAFE_PURGE_FILAMENT
    M83                            ; set extruder to relative
    G1 E100 F300                    ; extrude a little to soften tip
    G1 E-2 F1800                  ; retract some, but not too much or it will jam
    M82                            ; set extruder to absolute
    RESTORE_GCODE_STATE NAME=pre_UNSAFE_PURGE_FILAMENT